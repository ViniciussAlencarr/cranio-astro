---
import Layout from "../../layouts/Layout.astro";

// utils
import { Book } from "../../components/React/Book";

const { id } = Astro.params;

const baseUrl = import.meta.env.BASE_URL_API;
---

<Layout title="Livro">
    <Book id={id} baseUrl={baseUrl} client:load />
</Layout>
