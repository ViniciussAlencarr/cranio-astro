export interface ShoppingCart {
    id: string;
    bookTitle: string;
    bookAuthor: string;
    price: number;
    productId: string;
    publishedAt: string;
    updatedAt: string;
    createdAt: string;
    amount: number;
    name: string;
    author: string;
}